using UnityEngine;

public class LevelManager : MonoBehaviour
{
    private int _currentCustomerIndex = 0;
    private float _currentTime = 0f;

    public delegate void OnCustomerComplete();
     
}
